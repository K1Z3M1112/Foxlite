This directory is populated by GitHub Actions from the selected Winlator release.
The build workflow extracts the release APK's assets into app/src/main/assets/runtime/.

The runtime integration deliberately treats third-party runtime assets as replaceable components.
Do not commit redistributable binaries here unless their licenses permit it.
